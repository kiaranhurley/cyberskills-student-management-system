# Frontend

React + Vite UI for the Cyberskills Student Management System.

Setup and run instructions are in the [project root README](../README.md).

```bash
npm install
npm run dev
```

Requires the Django API on http://127.0.0.1:8000 (Vite proxies `/api` from port 3000).
